namespace Cortex.Contracts
{
    /// <summary>
    /// Marker type for the shared Cortex contract lane used by both the
    /// current runtime graph and future host or worker processes.
    /// </summary>
    public sealed class AssemblyMarker
    {
    }
}
